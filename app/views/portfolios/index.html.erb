<h1>Portfolio Items</h1>

<%= link_to "Create New Portfolio Item", new_portfolio_path %>

<% @portfolio_items.each do |portfolio_item| %>
<p><%= portfolio_item.title %></p>
<p><%= portfolio_item.subtitle %></p>
<p><%= portfolio_item.body %></p>
<p><%= image_tag(portfolio_item.thumb_image, alt:"Image of #{portfolio_item.title}") if portfolio_item.thumb_image %></p>
<p><%= link_to "Edit", edit_portfolio_path(portfolio_item.id) %></p>
<% end %>
